from django.contrib import admin
# from django.template.defaulttags import url
# from django.urls import path, include
# from rest_framework.routers import DefaultRouter
#
# from courses.views import *
#
# # from courses.views import CoursesAPIView
# router = DefaultRouter()
# router.register('branches', BranchesViewSet, basename='branch')
# router.register('contacts', ContactsViewSet, basename='contact')
# router.register('courses', ContactsViewSet, basename='course')
#
# urlpatterns = [
#     path('', include(router.urls))
#     # path('admin/', admin.site.urls),
#     # path('courses/1', CoursesAPIView.as_view()),
# ]